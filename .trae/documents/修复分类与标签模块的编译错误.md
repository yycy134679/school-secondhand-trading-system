### 修复方案

根据代码分析，我发现了三个编译错误，需要进行以下修复：

#### 1. 修复 `middleware.AdminMiddleware` 未定义问题

**问题**：`router/tag.go` 和 `router/category.go` 中使用了 `middleware.AdminMiddleware()`，但该函数未定义

**修复方案**：
- 创建 `middleware/admin.go` 文件，实现 `AdminMiddleware` 函数
- 或者修改现有 `auth.go`，添加 `AdminMiddleware` 函数
- 函数逻辑：检查用户角色是否为管理员

#### 2. 修复导入路径中的反斜杠问题

**问题**：`controller/tag/controller.go` 中的导入路径使用了反斜杠 `\`，导致编译错误

**修复方案**：
- 将绝对路径改为相对导入，使用项目模块名作为前缀
- 例如：`school-secondhand-trading-system/service/tag` 替代 `c:\Users\15751\Desktop\school-secondhand-trading-system\service\tag`

#### 3. 修复分类控制器的导入路径

**问题**：类似标签控制器，分类控制器也可能存在相同的导入路径问题

**修复方案**：
- 检查并修复 `controller/category/controller.go` 中的导入路径

### 修复步骤

1. **创建 `middleware/admin.go` 文件**，实现 `AdminMiddleware` 函数
2. **修复 `controller/tag/controller.go` 中的导入路径**
3. **修复 `controller/category/controller.go` 中的导入路径（如果存在相同问题）**
4. **运行编译检查**，验证修复是否成功

### 预期结果

- 所有编译错误消失
- 项目可以正常编译
- 分类与标签模块的功能正常可用